# API Reference

The generated API reference is published separately with GitHub Pages:

[Open the API reference](https://matthewdeanmartin.github.io/ai_shell/)

The API pages are generated from the `ai_shell` package with `pdoc` and written
to the `docs_api/` folder.
